import { TripIntakeWizard } from "@/components/trip-intake/TripIntakeWizard";

export default function PlanPage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-background to-muted py-12">
      <TripIntakeWizard />
    </div>
  );
}
